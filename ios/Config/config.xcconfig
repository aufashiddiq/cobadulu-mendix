PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_IDENTIFIER)$(BUNDLE_IDENTIFIER_APPEND)

BUNDLE_DISPLAY_NAME = CobaDulu2
BUNDLE_IDENTIFIER = com.myapp.cobadulu2

RUNTIME_URL = http:\/\/localhost:8000

PRODUCT_NAME = CobaDulu2
